import { Command } from "commander";
export declare function runCompiler({ watch, outdir }: {
    watch: boolean;
    outdir?: string;
}): Promise<void>;
declare const program: Command;
export default program;
