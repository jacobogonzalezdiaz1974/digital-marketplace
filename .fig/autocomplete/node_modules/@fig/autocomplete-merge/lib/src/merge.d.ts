import type { PresetName } from "./presets";
export interface MergeOptions {
    ignore?: {
        commandProps?: string[];
        optionProps?: string[];
        argProps?: string[];
        commonProps?: string[];
    };
    preset?: PresetName;
    prettifyOutput?: boolean;
    oldFileName?: string;
    newFileName?: string;
}
export declare function merge(oldFileContent: string, newFileContent: string, options: MergeOptions): Promise<string>;
